// components/analizler.tsx
import React from "react";

const Analizler: React.FC = () => {
  return (
    <div
      style={{
        textAlign: "center",
        padding: "40px",
        background: "#f9fafb",
        borderRadius: 10,
        color: "#374151",
        fontWeight: 600,
      }}
    >
      Yayında analiziniz yok.
    </div>
  );
};

export default Analizler;
