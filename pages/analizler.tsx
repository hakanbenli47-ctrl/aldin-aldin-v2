export default function Analizler() {
  return (
    <div
      style={{
        marginTop: "40px",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <div
        style={{
          background: "#f8fafc",
          padding: "40px",
          borderRadius: "12px",
          fontSize: "18px",
          fontWeight: "bold",
          color: "#475569",
          textAlign: "center",
        }}
      >
        Henüz analiz veriniz yok.
      </div>
    </div>
  );
}
